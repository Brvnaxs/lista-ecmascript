const {alunos} = require("./alunos")
const {cursos} = require("./cursos")
//Percorrendo lista de alunos.
for(i = 0; i < alunos.length; i++){
    console.log(alunos[i])
}
//Percorrendo lista de cursos
for(i = 0; i < cursos.length; i++){
    console.log(cursos[i])
}